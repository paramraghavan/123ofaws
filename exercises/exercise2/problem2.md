Given a aws s3 bucket and key,
pull all the data rows give year as the following row into a in memory db which we can query with simple sql

s3 bucket, job-name, year, month,day,file-name
s3 bucket + key
s3-bucket 
key staging/job_name/year/month/day/file-name
